<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Testing Three.js</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            overflow: hidden;
            margin: 0px;
            font-family: Arial, Helvetica, sans-serif;
            -webkit-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        main {
            width: 100vw;
            height: 100vh;
        }

        #overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: linear-gradient(135deg, rgba(31, 31, 49, 0), rgba(34, 3, 38, 0));
        }

        #overlay.active {
            display: block;
            background: linear-gradient(135deg, rgba(31, 31, 49, 0.8), rgba(34, 3, 38, 0.5));
        }

        #tryAgain {
            background-color: rgba(34, 3, 38, 0.5);
            color: white;
            padding: 10px 16px;
            border-radius: 4px;
            width: 30cqw;
            height: auto;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            font-weight: bolder;
            font-size: medium;
            z-index: 1000;
        }

        #playground {
            width: 100%;
            height: 100%;
            position: absolute;
        }

        #animationItems {
            transition: transform 0.1s ease;
            background: linear-gradient(135deg, rgba(31, 31, 49, 0.8), rgba(34, 3, 38, 0.5));
            padding: 10px;
            padding-right: 40px;
            margin: 10px;
            border-radius: 5px;
            font-size: 12px;
            z-index: 100;
        }

        #animationList {
            cursor: move;
            position: absolute;
            width: fit-content;
            height: fit-content;
        }

        #animationItems div {
            cursor: pointer;
            color: rgb(255, 255, 255);
            padding: 5px;
            transition: color 0.3s;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        #animationItems div:hover {
            color: rgb(90, 250, 234);
            transition: color 0.3s;
        }

        .returnButton {
            cursor: pointer;
            background: linear-gradient(180deg, rgba(34, 193, 195, 0.8), rgba(253, 187, 45, 0.7));
            color: white;
            margin: 10px;
            padding: 10px 16px;
            border-radius: 4px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            font-weight: 500;
            transform: scale(1);
            transition: transform 0.3s, background-color 0.3s;
        }

        .returnButton:hover {
            background-color: rgba(160, 247, 22, 0.9);
            transform: scale(1.1);
        }
    </style>
</head>

<body>